#include <Arduino.h>
#include <Goggles.h>

void setup() {}

void loop() {
  /*if (changeMode) {
    mode++;
    changeMode = false;
    if (mode > 6) {
      mode = 0;
    }
  }

  switch(mode) {
  case 0:
    blankAll();
    juggle(white);
    break;
  case 1:
    rollers(red);
    break;
  case 2:
    rollers(orange);
    break;
  case 3:
    rollers(green);
    break;
  case 4:
    rollers(cyan);
    break;
  case 5:
    rollers(blue);
    break;
  case 6:
    rollers(magenta);
    break;
  }*/
}
